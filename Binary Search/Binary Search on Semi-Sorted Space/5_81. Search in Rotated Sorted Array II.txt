A+


was really simple if you will look closey similar as rotated sorted array 1 
but with extra conditions which are really simple to get


class Solution {
public:
    bool search(vector<int>& nums, int target) {
        int left=0,right=nums.size()-1;

        while(left<=right){
            int mid = (left+right)/2;

            if(nums[mid]==target || nums[left]==target || nums[right]==target) return true;

            if (nums[mid] == nums[left]) {
                left++;
                continue;
            }
            
            if(nums[left] < nums[mid]){
                if(nums[left] <= target && nums[mid]>target)    right=mid-1;
                else left=mid+1;
            }
            else{
                if(mid+1 < nums.size() && nums[right] >= target && nums[mid+1]<=target)    left=mid+1;
                else right=mid-1;
            }
        }

        return false;
    }
};